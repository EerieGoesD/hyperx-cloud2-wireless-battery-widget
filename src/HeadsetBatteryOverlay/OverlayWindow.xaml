<Window x:Class="HeadsetBatteryOverlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Headset Battery Overlay"
        Width="220" Height="104"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <Border CornerRadius="10" Background="#CC111111" Padding="10" BorderBrush="#55FFFFFF" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel x:Name="LeftPanel" Grid.Column="0" VerticalAlignment="Center">
                <Viewbox Width="34" Height="34" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Grid Width="24" Height="24">
                        <Border CornerRadius="2" BorderBrush="White" BorderThickness="2" Background="Transparent" Margin="0,2,3,2"/>
                        <Border CornerRadius="1" Background="White" Width="4" Height="8" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        <Rectangle x:Name="BatteryFill" Fill="LimeGreen" Margin="3,5,6,5" RadiusX="1" RadiusY="1" HorizontalAlignment="Stretch"/>
                    </Grid>
                </Viewbox>

                <TextBlock Margin="0,6,0,0" FontSize="11" Foreground="#CCFFFFFF" HorizontalAlignment="Center">
                    <Hyperlink NavigateUri="https://linktr.ee/eeriegoesd" RequestNavigate="Hyperlink_RequestNavigate">Made by EERIE</Hyperlink>
                </TextBlock>

                <TextBlock Margin="0,2,0,0" FontSize="11" Foreground="#CCFFFFFF" HorizontalAlignment="Center">
                    <Hyperlink NavigateUri="https://buymeacoffee.com/eeriegoesd" RequestNavigate="Hyperlink_RequestNavigate">Buy Me a Coffee</Hyperlink>
                </TextBlock>
            </StackPanel>

            <StackPanel x:Name="RightPanel" Grid.Column="2" VerticalAlignment="Center">
                <StackPanel x:Name="PercentRow" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock x:Name="PercentText" Text="--%" Foreground="White" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button x:Name="BtnCompact" Content="-" Width="18" Height="18" FontSize="12"
                            Padding="0" Margin="8,0,0,0"
                            Background="Transparent" Foreground="#CCFFFFFF"
                            BorderBrush="#55FFFFFF" BorderThickness="1"
                            Click="BtnCompact_Click"/>
                    <Button x:Name="BtnExpand" Content="+" Width="18" Height="18" FontSize="12"
                            Padding="0" Margin="6,0,0,0"
                            Background="Transparent" Foreground="#CCFFFFFF"
                            BorderBrush="#55FFFFFF" BorderThickness="1"
                            Click="BtnExpand_Click"
                            Visibility="Collapsed"/>
                </StackPanel>

                <TextBlock x:Name="StatusText" Text="Starting..." Foreground="#CCFFFFFF" FontSize="11" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
